**/.git
**/.gitignore
**/node_modules
**/*.log

# TypeScript incremental build info — must be absent so tsc emits fresh output in Docker
**/tsconfig.tsbuildinfo

# Compiled output — rebuilt inside Docker
**/dist

# Test / infra
tests/
infra/
docs/
scripts/
# Bootstrap image needs these two scripts baked in
!scripts/db/migrate.sh
!scripts/db/bootstrap-admin.sh
*.md
