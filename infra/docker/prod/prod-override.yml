# Prod-only overrides (security, env). Use: docker compose -f compose.yml -f prod-override.yml up -d
# env_file: .env.prod (bu klasördeki .env.prod)
services:
  postgres:
    env_file: .env.prod
    # Do NOT expose postgres ports publicly in prod
    ports: !reset []
    expose:
      - "5432"
    read_only: false
    security_opt:
      - no-new-privileges:true
    tmpfs:
      - /tmp

  redis:
    env_file: .env.prod
    # Do NOT expose redis ports publicly in prod
    ports: !reset []
    expose:
      - "6379"
    security_opt:
      - no-new-privileges:true
    user: redis

  bootstrap:
    env_file: .env.prod

  cp:
    env_file: .env.prod
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      bootstrap:
        condition: service_completed_successfully
    read_only: true
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    cap_add:
      - NET_BIND_SERVICE
    tmpfs:
      - /tmp

  dp:
    env_file: .env.prod
    read_only: true
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    cap_add:
      - NET_BIND_SERVICE
    tmpfs:
      - /tmp
      - /home/pwuser/.cache

  admin-portal:
    env_file: .env.prod
    read_only: true
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    cap_add:
      - NET_BIND_SERVICE
    tmpfs:
      - /tmp

  staff-portal:
    env_file: .env.prod
    read_only: true
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    cap_add:
      - NET_BIND_SERVICE
    tmpfs:
      - /tmp

  caddy:
    env_file: .env.prod
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    cap_add:
      - NET_BIND_SERVICE

      - /tmp
