# Stage: build and push Docker images only. No .env used here.
# Deploy (deploy.yml) runs on the server and uses prod/.env.prod (see deploy step summary).
name: Stage

on:
  push:
    branches: [main]
  workflow_dispatch:

env:
  REGISTRY: ghcr.io

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v4

      - uses: docker/setup-buildx-action@v3

      - uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push CP
        uses: docker/build-push-action@v6
        with:
          context: .
          file: apps/cp/Dockerfile
          target: production
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ github.repository_owner }}/visa-cp:${{ github.sha }}
            ${{ env.REGISTRY }}/${{ github.repository_owner }}/visa-cp:${{ github.ref_name }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Build and push DP
        uses: docker/build-push-action@v6
        with:
          context: .
          file: apps/dp/Dockerfile
          target: production
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ github.repository_owner }}/visa-dp:${{ github.sha }}
            ${{ env.REGISTRY }}/${{ github.repository_owner }}/visa-dp:${{ github.ref_name }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Build and push Admin Portal
        uses: docker/build-push-action@v6
        with:
          context: .
          file: apps/web/admin-portal/Dockerfile
          target: production
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ github.repository_owner }}/visa-admin-portal:${{ github.sha }}
            ${{ env.REGISTRY }}/${{ github.repository_owner }}/visa-admin-portal:${{ github.ref_name }}
          build-args: |
            NEXT_PUBLIC_CP_API_URL=https://manager.vizeself.com
            NEXT_PUBLIC_API_URL=https://manager.vizeself.com
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Build and push Staff Portal
        uses: docker/build-push-action@v6
        with:
          context: .
          file: apps/web/staff-portal/Dockerfile
          target: production
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ github.repository_owner }}/visa-staff-portal:${{ github.sha }}
            ${{ env.REGISTRY }}/${{ github.repository_owner }}/visa-staff-portal:${{ github.ref_name }}
          build-args: |
            NEXT_PUBLIC_CP_API_URL=https://portal.vizeself.com
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Build and push Mock Portal
        uses: docker/build-push-action@v6
        with:
          context: .
          file: apps/web/mock-portal/Dockerfile
          target: production
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ github.repository_owner }}/visa-mock-portal:${{ github.sha }}
            ${{ env.REGISTRY }}/${{ github.repository_owner }}/visa-mock-portal:${{ github.ref_name }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
